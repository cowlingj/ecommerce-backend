env: production
name: cms
group: frontend
image: "gcr.io/gke-test-253221/cms.admin"
imagePullSecret: gcr
version: "0.0.1"
# mongodbUrl: [required]
cookieSecret: tmp
ports:
  ui:
    port: 80
    protocol: http
    name: ui
  api:
    port: 8080
    protocol: http
    name: api
