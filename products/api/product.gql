type Product {
  id: ID!
  name: String!
  imageUrl: URL
  price: ProductPrice!
}

type ProductPrice {
  value: Number!
  currency: String!
}

extend type Query {
  allProducts: [Product]
  productById(id: ID!): Product
}
