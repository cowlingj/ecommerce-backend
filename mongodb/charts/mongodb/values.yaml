enabled: true
env: production
name: mongodb
group: frontend
image: gcr.io/gke-test-253221/mongodb.data
imagePullSecret: gcr
version: "4.2.0"
persistenceDiskName: mongodb-disk
ports:
  main:
    port: 8080
    protocol: mongo
    name: main
