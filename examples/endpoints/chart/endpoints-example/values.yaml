replicaCount: 1

image:
  repository: gcr.io/gke-test-253221/endpoints.example
  tag: 0.0.1
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

service:
  nameOverride: ""
  fullnameOverride: ""
  type: ClusterIP
  ports:
    main: 80
    status: 8080

resources: {}

livenessProbe: |-
  httpGet:
    path: {{ printf "\"%s%s\"" .Values.basePath "/status" }}
    port: http-status

readinessProbe: |-
  httpGet:
    path: {{ printf "\"%s%s\"" .Values.basePath "/status" }}
    port: http-status

startupProbe: |-
  httpGet:
    path: {{ printf "\"%s%s\"" .Values.basePath "/status" }}
    port: http-status

nodeSelector: {}

tolerations: []

affinity: {}

basePath: /

endpoints: { "endpoints": [] }

admin:
  email: admin@localhost
